# Project Mwavuli - Dependencies
# Content moderation and verification system

# Web Framework
fastapi>=0.109.0
uvicorn>=0.27.0
pydantic>=2.5.0

# AI/ML Models
detoxify>=0.5.2
torch>=2.9.0
transformers>=4.36.0

# Google Gemini (new API preferred, old API as fallback)
google-genai>=0.2.0
google-generativeai>=0.3.0  # Fallback for compatibility

# Firebase (required when DB_PROVIDER=firebase)
firebase-admin>=6.4.0

# Supabase (required when DB_PROVIDER=supabase)
supabase>=2.0.0

# Twilio (WhatsApp)
twilio>=9.0.0

# HTTP client (media verification)
httpx>=0.27.0

# Utilities
python-dotenv>=1.0.0

# Web ingestion
feedparser>=6.0.10
requests>=2.31.0
trafilatura>=1.6.0

# Kaggle import script
kagglehub
pandas

# Training (optional — needed for scripts/train_kenyan_classifier.py)
datasets>=2.16.0
scikit-learn>=1.3.0
accelerate>=1.1.0

# Embeddings (optional — needed for semantic coordination detection)
sentence-transformers>=2.2.0

# Media analysis (optional — image OCR, audio transcription)
pytesseract>=0.3.10
Pillow>=10.0.0
openai-whisper>=20231117

# SMS intake (optional — Africa's Talking)
africastalking>=1.2.5

# Queue processing (optional — Redis)
redis>=5.0.0

# HuggingFace model fine-tuning
# (transformers already listed above)
