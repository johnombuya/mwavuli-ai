# Backend URL (only needed if not using Next.js rewrites)
NEXT_PUBLIC_BACKEND_URL=

# API key for authenticating with the backend (empty = no key sent)
# In production, set this to a viewer-role key from your backend API_KEYS
NEXT_PUBLIC_API_KEY=
